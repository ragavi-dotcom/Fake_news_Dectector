# ============================================================
# ALL Python libraries for the Fake News Detector project
# Install with:  pip install -r requirements.txt
# ============================================================

# --- Data handling ---
pandas==2.2.2
numpy==1.26.4

# --- Machine learning (training + inference) ---
scikit-learn==1.5.0
joblib==1.4.2

# --- Text preprocessing ---
nltk==3.8.1

# --- Plots (used in the notebook only) ---
matplotlib==3.9.0
seaborn==0.13.2

# --- Notebook environment ---
jupyter==1.0.0
notebook==7.2.1

# --- Backend API (FastAPI serves the model) ---
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4

# --- Explainability (shows which words drove each prediction) ---
lime==0.2.0.1

# --- CORS so the React frontend can call the API ---
# (comes bundled with fastapi via starlette, no extra install)

# --- Database driver (SQLite is built into Python; add these only if you switch DB) ---
# psycopg2-binary==2.9.9   # PostgreSQL
# pymysql==1.1.1           # MySQL
